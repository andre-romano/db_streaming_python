<!-- atualizar/video.jinja2 -->

{% extends "_base.jinja2" %}

{% block nav_title %}STREAMING{% endblock %}
{% block title %}Atualizar{% endblock %}

{% block content %}

<h2 class="text-center" style="margin-bottom: 20px;">
    Atualizar Vídeo
</h2>

<form class="text-center" action="{{ api }}" method="post">

    {{ macros.render_input("", "hidden", "id", id) }}
    {{ macros.render_input("Ano:", "number", "ano", ano) }}
    {{ macros.render_input("Titulo:", "text", "titulo", titulo) }}
    {{ macros.render_input("Sinopse:", "text", "sinopse", sinopse) }}
    {{ macros.render_input("Duracao:", "time", "duracao", duracao) }}
    {{ macros.render_select("Classificacao:", "id_classificacao", classificacoes, id_classificacao) }}

    <button type="submit" class="btn btn-primary">Atualizar</button>
</form>

{% endblock %}